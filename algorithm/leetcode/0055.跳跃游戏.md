class Solution {
public:
    bool canJump(vector<int>& nums) {
        int n=nums.size();
        int maxn=0;
        for(int i=0;i<n-1;i++){
            if(i<=maxn)maxn=max(maxn,i+nums[i]);
        }
        return maxn>=n-1;
    }
};
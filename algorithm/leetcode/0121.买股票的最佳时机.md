```cpp
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int ans=0,minn=prices[0];
        for(int num:prices){
            minn=min(num,minn);
            ans=max(ans,num-minn);
        }
        return ans;
    }
};